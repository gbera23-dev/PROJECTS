
int x = 5; 

if (x) {; 
    int y = 3; 
    CRC_OUT x; 
    x = x - 1; 
    if( x) {; 
        short z = 10; 
        CRC_OUT x; 
        x = x - 1; 
        if ( x) {; 
            CRC_OUT x; 
            x = x - 1; 
            if (x) {; 
                CRC_OUT x; 
                x = x - 1; 
                if (x) {; 
                    CRC_OUT x; 
                    x = x - 1; 
                    if (x) {; 
                        CRC_OUT x; 
                        x = x - 1; 
                        if (x ) {; 
                            CRC_OUT x; 
                            x = x - 1; 
                        }; 
                    };
                };
            }; 
        };
    };
}; 

CRC_OUT x; 


void main() {;
};